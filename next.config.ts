import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Cloudflare Pages compatibility - use edge runtime where possible
  // Note: Some features may need adjustment for Cloudflare
};

export default nextConfig;
